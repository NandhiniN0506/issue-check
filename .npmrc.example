# Copy to .npmrc and replace with your real tokens (never commit .npmrc!)
# npm config for Chargebee private packages

@chargebee:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=YOUR_GITHUB_PAT_HERE
